{% trans_default_domain 'FOSUserBundle' %}

<form class="form-signin" action="{{ path('fos_user_group_edit', {'groupName': group_name} ) }}" {{ form_enctype(form) }} method="POST" class="fos_user_group_edit">
    {{ form_widget(form) }}
    <button class="btn btn-lg btn-primary btn-block" type="submit" id="_submit" name="_submit" >Sign in</button>

</form>
